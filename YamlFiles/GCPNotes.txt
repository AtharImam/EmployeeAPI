gcloud auth login
gcloud config set project kubernetestest-464810
gcloud container clusters create my-cluster --zone us-central1-a --num-nodes 1 --disk-size 12 --disk-type pd-standard --machine-type e2-medium --enable-ip-alias --project kubernetestest-464810
gcloud container clusters get-credentials autopilot-cluster-1 --region us-central1 --project kubernetestest-464810

kubectl get pods -l app=employeeapi --watch
kubectl set image deployment/emplapi-depl employeeapi=nginx:1.25-alpine
kubectl rollout status deployment emplapi-depl
kubectl exec -it empldb-ssets-0 -- mysql -h empldb-ssets-0.empldb-hl -u root -p
kubectl get pods -l app=mysql --watch
docker run --rm williamyeh/hey -n 4000 -c 200 http://35.201.101.217/api/employee
kubectl exec -it empldb-ssets-0 -- mysql -h empldb-ssets-0.empldb-hl -u root -p EmployeeDb
kubectl exec -it empldb-ssets-0 -- mysql -h empldb-ssets-0.empldb-hl -u root -p EmployeeDb -e "SELECT * FROM Employees;"
docker exec -it empldb-ssets-0 sh -c 'MYSQL_PWD=$MYSQL_ROOT_PASSWORD mysql -u root -e "SELECT ATTR_VALUE, COUNT(1) Connections FROM performance_schema.session_connect_attrs WHERE ATTR_NAME = '\''program_name'\'' GROUP BY ATTR_VALUE;"'
docker exec -it empldb-ssets-0 sh -c 'MYSQL_PWD=$MYSQL_ROOT_PASSWORD mysql -u root -e "SELECT * FROM Employees;" your_EmployeeDb'


SELECT ATTR_VALUE, COUNT(1) Connections FROM performance_schema.session_connect_attrs WHERE ATTR_NAME = 'program_name' GROUP BY ATTR_VALUE;


| Tool                      | Performance          | Notes                                    |
| ------------------------- | -------------------- | ---------------------------------------- |
| **OBS Studio**            | ✅ Good (with tuning) | Free, customizable, GPU support          |
| **ShareX**                | ✅ Light              | Free, open-source, basic editing         |
| **Loom**                  | ✅ Cloud-based        | No system slowdown, limited free tier    |
| **Xbox Game Bar** (Win+G) | ✅ GPU-accelerated    | Built-in, efficient for screen recording |

